# Organization Configuration (generated by synapse init)
# This file is populated during initialization based on selected modules
organization:
  name: "{{ORG_NAME}}"
  ceo: "{{CEO_NAME}}"
  president: "{{PRESIDENT_NAME}}"

teams: []
  # Populated from enabled modules during init
